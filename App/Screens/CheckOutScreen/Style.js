import React from 'react';
import {StyleSheet} from 'react-native';
import colors from '../../config/colors';

const styles = StyleSheet.create({
  Total: {
    left: 120,
    color: colors.dark,
    fontWeight: 'bold',
  },
  txt: {
    color: colors.mediumGrey,
  },
  endbox: {backgroundColor: colors.white},
});
export default styles;
